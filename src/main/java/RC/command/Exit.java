package RC.command;

import RC.TaskList;
import RC.UI.Ui;

/**
 * Represents a command to exit RC chatbot.
 */
public class Exit extends RCCommand {
    public Exit() {
    }

    @Override
    public void execute(TaskList taskList, Ui ui) {
    }
}
