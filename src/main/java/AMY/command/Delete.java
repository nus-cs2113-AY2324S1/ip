package AMY.command;

public class Delete extends Task {

    protected String by;

    public Delete(String description, String by) {
        super(description);
        this.by = by;
    }

    @Override
    public String toString() {
        return "[D]" + super.toString() + " (by: " + by + ")";
    }
}
